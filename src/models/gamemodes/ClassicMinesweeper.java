package models.gamemodes;

import models.Minesweeper;
import models.features.Difficulty;

/**
 * A model for the Classic game mode
 */

public class ClassicMinesweeper extends Minesweeper {
    public ClassicMinesweeper (Difficulty difficulty) {
        super(difficulty);
    }
}
